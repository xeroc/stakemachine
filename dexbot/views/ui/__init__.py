# This package includes the .ui files and the generated *_ui.py files.
# *_ui.py files are generated and should not be edited. The files are automatically generated when
# `python setup.py install` is ran, or by running `python setup.py build_ui`
